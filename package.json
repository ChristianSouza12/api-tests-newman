{
  "name": "api-tests-newman",
  "version": "1.0.0",
  "description": "Testes automatizados de API com Postman, Newman e CI/CD",
  "scripts": {
    "test": "newman run collection.json -e environment.json",
    "report": "newman run collection.json -e environment.json -r cli,htmlextra --reporter-htmlextra-export newman-report.html"
  },
  "author": "Christian Souza",
  "license": "ISC",
  "dependencies": {
    "newman": "^6.2.2",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}